@model IEnumerable<FretesWebApplication.Models.Veiculo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Denominacao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipoVeiculo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PesoVeiculo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Denominacao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoVeiculo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PesoVeiculo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdVeiculo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdVeiculo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdVeiculo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
