@model IEnumerable<FretesWebApplication.Models.Veiculo>

@{
    ViewData["Title"] = "Index";
}

<h1>Veículos</h1>

<p>
    <a asp-action="Create">Adicionar Novo Veículo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Denominacao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipoVeiculo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PesoVeiculo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Denominacao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoVeiculo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PesoVeiculo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdVeiculo">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.IdVeiculo">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.IdVeiculo">Excluir</a>
            </td>
        </tr>
}
    </tbody>
</table>
